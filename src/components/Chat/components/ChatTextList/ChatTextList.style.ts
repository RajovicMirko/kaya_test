import { Box, styled } from "@mui/material";

export const ChatTextListStyle = styled(Box)(() => {
  return {
    width: "100%",
    height: "100%",
    overflow: "hidden",
    overflowY: "auto",
  };
});
